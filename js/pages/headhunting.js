// headhunting.js - AI 매칭 서비스 페이지 모듈

export function init() {
    console.log('Headhunting page module initialized');

    // AI 매칭 서비스 페이지별 초기화가 필요한 경우 여기에 추가
    // 현재는 기존 headhunting.js가 모든 기능을 처리하므로
    // 추가적인 모듈 초기화만 수행

    // 페이지 로드 완료 표시
    document.body.classList.add('page-loaded');
}
